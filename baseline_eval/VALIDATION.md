# 交付前验证

执行环境：Python 3.12.13；langchain-core 1.3.3。使用本地替身模型和工具，没有发起真实模型或检索请求。

执行命令：

```bash
python -m unittest discover -s baseline_eval -p test_runner.py -v
```

6项测试全部通过：

1. 通过实际LangChain Runnable、工具和替身聊天模型验证嵌套回调传播，保存完整工具返回及用量。
2. 部分调用缺失Token时，完整Token总数保持未知，同时保留已知小计。
3. 函数吞掉异常并上报错误时，错误事件仍被保留，不将其视为有正常回答。
4. 初始化异常留下明确状态和错误信息。
5. 未评分、未跑完批次不会生成完整通过率。
6. 主进程超时后仍保存本题结果和批次汇总。

另已执行 `python baseline_eval/run.py --dry-run`，确认三道题与附件存在并可预览。

这些结果验证评测记录工具的基本行为，不代表原项目已经在真实API环境运行成功，也不代表Agent完成了任何测试题。实际DeepAgents及外部服务兼容性仍需在用户原有依赖环境中完成首次T02试运行。
